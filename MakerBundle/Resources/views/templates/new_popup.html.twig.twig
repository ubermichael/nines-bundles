{% autoescape false %}
    {{ "{% extends 'popup.html.twig' %}" }}

    {{ '{% block title %}' }}New {{ |camel_title|camel_title }} {{ '{% endblock %}' }}

    {{ "{% block body %}" }}
    <h1>New {{ entity_class_name|camel_title }}</h1>

    {{ '{{ form_start(form) }}' }}
    {{ '{{ form_widget(form) }}' }}
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <input type="submit" value="Create" class="btn btn-primary" />
            <a href="{{ "{{ path('" ~ route_name ~ "_index') }}" }}" class="btn btn-warning">Cancel</a>
        </div>
    </div>
    {{ '{{ form_end(form) }}' }}

    {{ "{% endblock %}" }}

    {{ "{% block javascripts %}" }}
    {{  "{% include '@NinesEditor/editor/widget.html.twig' %}" }}
    {{ "{% endblock %}" }}
{% endautoescape %}
