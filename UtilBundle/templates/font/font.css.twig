
/* {{ family|raw }} {{ style }} {{ weight }}*/
@font-face {
    font-family: {{ family|raw }};
    font-style: {{ style }};
    font-weight: {{ weight }};
    src: {% for local in locals -%}local('{{ local|raw }}'),{%- endfor %}

{% for format, filename in formats %}
    url('{{ (prefix ~ '/' ~ filename)|raw }}') format('{{ format }}'){% if not loop.last %},{% else %};{% endif %}

{% endfor %}
}
