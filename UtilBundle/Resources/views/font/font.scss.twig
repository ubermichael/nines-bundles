
/* {{ name }} */
@font-face {
    font-family: {{ family|raw }};
    font-style: {{ style }};
    font-weight: {{ weight }};
    src: {% for local in locals -%}local('{{ local }}'),{%- endfor %}

{% for format, path in formats %}
    url('{{ asset(path) }}') format('{{ format }}'){% if not loop.last %},{% else %};{% endif %}

{% endfor %}
}
